const root=document.documentElement;const buttons=[...document.querySelectorAll('[data-theme]')];const apply=theme=>{root.classList.remove('light','dark','system');root.classList.add(theme);buttons.forEach(button=>button.classList.toggle('active',button.dataset.theme===theme));localStorage.setItem('three-axes-theme',theme)};buttons.forEach(button=>button.addEventListener('click',()=>apply(button.dataset.theme)));apply(localStorage.getItem('three-axes-theme')||'system');
